@import "../../styles/form-cell.wxss";

.edit-page {
  background: var(--bg);
  min-height: 100vh;
  padding-bottom: 60rpx;
}

.edit-header {
  padding: 40rpx 32rpx 24rpx;
}

.edit-title {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text-main);
}

.edit-form {
  padding: 24rpx 32rpx;
}
